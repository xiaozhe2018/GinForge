# admin-api 服务生产环境配置

services:
  admin_api:
    port: 8083

# 日志配置
log:
  level: "info"
  output: "file"
  file_path: "/var/log/ginforge/admin-api.log"

# 服务特定配置
admin:
  default_page_size: 20
  max_page_size: 100

